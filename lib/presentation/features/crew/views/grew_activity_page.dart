import 'package:flutter/material.dart';

class GrewActivityPage extends StatelessWidget {
  const GrewActivityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("GrewActivity")),
      body: Center(child: Text("GREW 활동")),
    );
  }
}
