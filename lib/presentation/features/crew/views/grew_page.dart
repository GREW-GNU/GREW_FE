import 'package:flutter/material.dart';

class GrewPage extends StatelessWidget {
  const GrewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Grew")),
      body: Center(child: Text("Grew")),
    );
  }
}
